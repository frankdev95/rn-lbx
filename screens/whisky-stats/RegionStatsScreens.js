import React from "react";
import { View, Text, StyleSheet } from "react-native";

const RegionStatsScreen = ({}) => {
  return (
    <View>
      <Text>Region Stats Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default RegionStatsScreen;
